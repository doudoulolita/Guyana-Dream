
// fonction permettant de charger les images

function prechargerImage(url)
{
        var img = new Image();
    	img.src=url;
        return img;
}

function prechargerImages(img, url)
{
        var img = new Image();
    	img.src=url;
        return img;
}

